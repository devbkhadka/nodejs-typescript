{
  "name": "nodejs-typescript",
  "version": "1.0.0",
  "description": "1. Let's create a working folder first    mkdir nojes-typescript",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "node --inspect-brk --require ts-node/register index.ts",
    "start": "node --require ts-node/register index.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^10.5.1",
    "ts-node": "^7.0.0",
    "typescript": "^2.9.2"
  }
}
